---
description: Audit and migrate active personal Codex configuration.
---

Use `optimize-my-codex` for `$ARGUMENTS`.

Inventory active source, exclude generated cache from direct edits, and
preserve learned behavior plus explicit provider/model choices. Present the
exact migration for approval, then apply approved changes automatically and
retain baseline/validation evidence under an appropriate `.codex/specs/`
directory.
