interface:
  display_name: "Git Workflow"
  short_description: "Non-interactive Git branches, commits, and PRs"
  default_prompt: "Use $git-workflow to perform this Git task safely and non-interactively."

policy:
  allow_implicit_invocation: true
